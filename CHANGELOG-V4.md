# V4更新记录

## 2026-07-17 公开目录与 GitHub Pages

- 将 1,482 条公开两轮索引生成脱敏浏览目录，共覆盖来源所述 3,943 个款型；索引可能混有电动自行车、摩托车、非道路和历史车型，不再把总数直接称作摩托车数量。原始全文、详情候选和审核记录仍不进入生产构建。
- 目录明确分为 422 条停售、24 条大陆未引进、5 条即将上市、5 条暂无公开报价、13 条来源标注新车上市和 1,013 条有历史/公开指导价。
- 新增车型搜索、状态筛选、分批加载和逐条来源链接；所有目录项固定不进入推荐，页面明确公开报价不等于当前有货或当地可上牌。
- 明确道路准入与市场供应双状态口径；当前双重核验具体版本为 0，33 条基础数据继续标明为合并推荐示例组。
- 新增目录安全校验、覆盖口径文档、GitHub Pages 自动发布工作流和完整部署教程。
- 推荐引擎新增 `recommendable: false` 硬门；即使待审核目录记录误传入打分，也会被强制排除。主站测试增至 22 项。
- 完成 13 条“新车上市”车型族的一手检索初核并接入目录筛选；市场证据、道路准入、候选工厂型号和来源链接分别展示，当前 0 条通过正式闭环。
- 继续完成 5 条“即将上市”和 5 条“暂无报价”初核，总进度增至 23 条；纠正 AE4i、Fz5 110 为电动自行车，并拆分 GN125、DM125、HJ125-8 等旧聚合名。
- 新增精确版本双状态审核账本和自动生成白名单；中国大陆官方供应、工信部记录、CCC、证据时效、关键推荐参数和人工复核缺一不可。
- 将引擎资格门改为默认拒绝：只有显式 `recommendable: true` 才能进入具体推荐。33 条基础示例与第三方候选已从结果、复制文案和海报隔离；白名单为空时只输出车型方向。
- 收紧生产构建白名单：`dist/` 不再包含基础示例数据或第三方候选模块，生产界面只公开示例数量元数据。
- 当前自动测试合计 56 项：正式账本 15 项、推荐/场景 23 项、采集管道 18 项；生产构建通过。
- 目录中的“新车上市”和价格改为第三方索引标注，并让一手初核阻断结论优先显示，避免把索引价误读为官方在售价。

## 2026-07-16 续跑加固

- 修复自动进入下一题与手动“下一步”叠加导致的跨题跳转，并在返回首页、继续、重来和上一步时清理计时器。
- 手机端修复首屏标题断行、题目回到顶部、数据入口隐藏和安全区留白；补齐按钮选择态、进度条和弹窗的无障碍语义。
- 键盘支持数字键、Enter/Space 选项、Enter 继续和车型卡片打开详情；返回首页后可继续上次进度。
- 新手高动力车型、明确无 ABS 车型、矮个高座/超重车型，以及维修、热、重、贵、无聊等“一票否决”项改为硬排除。
- 示例聚合车型不再显示高可信度；缺价不再伪装成 `[0,0]`、低持有成本或低动力。
- 结果页补充具体反向排除车型，海报底部改为个性化现实提醒，继续保持 900×1200 输出。
- 新增关键路径、边界场景、缺价与采集安全测试；主站 21 项、采集管道 18 项。
- 摩托范管道改为固定工作路径、低速串行、访问阻断即停、字段白名单、严格 30 条上限和两阶段人工审核发布。
- 完成 30 条详情与参数页试采；只保留所有版本一致的单值规格，未知类别、持有成本、维护、外观和动力全部留空。
- 构建改为公开数据白名单，raw、details 和 pending 候选不会进入 `dist/`。

## 交互

- 10轮访谈扩展为12轮。
- 新增骑行经验。
- 新增通勤距离和停车环境。
- 保留点击单选后自动进入下一题。
- 保留数字键、Enter和左方向键。

## 推荐引擎

- 类型倾向分与具体车型分离。
- 加入预算硬过滤。
- 加入座高、车重和新手风险过滤。
- 加入停车环境与高价值车型冲突。
- 加入老车13年保守筛选。
- 每台车返回匹配理由、现实代价、硬性排除原因和数据可信度。
- 推荐结果增加品牌和车型大类去重。

## 结果页

- 增加第二倾向。
- 增加“必须保留/必须接受/重点核验”。
- 增加车型详情弹窗。
- 增加3:4结果海报。
- 增加车型数据状态面板。

## 工程化

- 配置、数据、引擎、存储、海报和UI拆分。
- 增加Node测试。
- 增加数据结构检查。
- 增加无依赖本地服务器。
- 增加构建脚本和Codex执行文件。
